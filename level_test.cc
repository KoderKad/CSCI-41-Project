#include <iostream>
#include <cmath>
using namespace std;

//Dylan- this is what I have so far, feel free to add or change to fit the rest

const int LevelCap = 100;
const int StartingLevel = 1;


void LevelUp(int level, int exp, int expNeeded)(
	level++;
	totalExp =- expNeeded;

)

void ExpGain(int level, int exp, int expNeeded) (
	if() Levelup(level, exp);

)

int main() {
	int expNeeded;
	int totalExp;
	int expNeeded = 100*level
}
